import {StyleSheet} from 'react-native';
import { vh } from '../../utils/units';

const styles = StyleSheet.create({
  mainView: {flex: 1, margin: 5 * vh},
  headingText: {
    fontWeight: 'bold',
    paddingBottom: 1 * vh
  },
});

export default styles;
